package algorithm;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main_1799 {
	
	static int[][] arr;
	static int n;
	
	public static void main(String[] args) throws NumberFormatException, IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		n = Integer.parseInt(br.readLine());
		
		for (int i = 0; i < n; i++) {
			String[] str = br.readLine().split(" ");
			for (int j = 0; j < n; j++) {
				arr[i][j] = Integer.parseInt(str[j]);
			}
		}
		
		dfs(0, 0, 0);
		
	
	}

	private static void dfs(int x, int y, int type) {
		if(x >= n) {
			y += 1;
			x = 0;
		}
		
		if(y >= n) {
			return;
		}
		
		if((x + y) % 2 != type) {
			dfs(x + 1, y, type);
			return;
		}
		
		if((x + y) % 2 == type && check(x, y, type) && arr[y][x] == 1) {
			arr[y][x] = 2;
			dfs(x + 2, y, type);
			arr[y][x] = 1;
		}
		dfs(x + 2, y, type);
	}

	private static boolean check(int x, int y, int type) {
		for (int i = 0; i < n; i++) {
			for (int j = 0; j < n; j++) {
				if((i + j) % 2 == type && arr[i][j] == 2 && Math.abs(i - y) == Math.abs(j - x)) {
					return false;
				}
			}
		}
		
		return true;
		return false;
	}
}